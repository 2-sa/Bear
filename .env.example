# Optional public deployment overrides. The API proxy defaults to the
# project-owned Worker; the other features stay disabled until configured.
VITE_HARBOR_WORKER_PROXY_BASE=
VITE_HARBOR_PUBLIC_APP_ORIGIN=
VITE_HARBOR_PUBLIC_RELAY_URL=
VITE_HARBOR_PUBLIC_SITE_ORIGIN=
VITE_HARBOR_ASSET_ORIGIN=
